# Optional local Qdrant for a future vector store migration.
# The default app uses Chroma under ./data/chroma (no Docker required).

services:
  qdrant:
    image: qdrant/qdrant:v1.13.4
    ports:
      - "6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
